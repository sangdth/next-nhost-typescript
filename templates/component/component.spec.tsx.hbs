import React from 'react';
import { render, screen } from '@/test/utils';
import { {{ name }} } from './{{ name }}';

describe('{{ name }}', () => {
  it('renders text', () => {
    render(<{{ name }} something="lorem" />);
    expect(screen.getByText('{{name}}')).toBeDefined();
  });
});
