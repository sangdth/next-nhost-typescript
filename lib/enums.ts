export enum Position {
  Top = 'TOP',
  Down = 'DOWN',
  Left = 'LEFT',
  Right = 'RIGHT',
}
export enum Entity {
  Users = 'USERS',
  Options = 'OPTIONS',
}
export enum Status {
  Public = 'PUBLIC',
  Private = 'PRIVATE',
  Archived = 'ARCHIVED',
}
